# FastAPI backend on Render. Must be reachable from the browser, so it has to
# be NEXT_PUBLIC_. No secrets belong here - the service-role key stays on the
# backend and is never exposed to the client.
NEXT_PUBLIC_API_URL=http://localhost:8000
